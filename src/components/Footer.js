import PropTypes from 'prop-types'

const Footer = (props) => {
    return (
        <>
            {/* The footer for the whole website */}
        </>
    )
}

Footer.defaultProps = {
    title: 'Covid Support',
}

Footer.propTypes = {
    title: PropTypes.string,
}

// CSS in reactJS
// const headingStyle = {
//     color: 'red',
//     backgorundColor: 'black',
// }

export default Footer